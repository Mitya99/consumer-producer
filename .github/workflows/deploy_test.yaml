name: Deploy_test

on:
  push:
    branches: [ main ]

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Deploy with Docker Compose
        run: |
          docker-compose down
          docker-compose up -d --build
